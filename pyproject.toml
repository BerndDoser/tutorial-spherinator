[project]
name = "tutorial-spherinator"
version = "0.1.0"
description = "Tutorial for Spherinator"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ipykernel>=7.3.0",
    "spherinator",
]

[tool.uv.sources]
spherinator = { git = "https://github.com/HITS-AIN/Spherinator.git"}
